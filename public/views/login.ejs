<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/global.css" rel="stylesheet">
    <link href="/css/header.css" rel="stylesheet">
    <link href="/css/footer.css" rel="stylesheet">
    <link href="/css/login.css" rel="stylesheet">
    <title>Diablo 2 Challenge - Login</title>
</head>
<body>
    <%- include('partials/header') %>

    <h2>Login</h2>

    <form action="/login" method="POST">
        <input type="text" name="loginid" id="loginid" placeholder="Username or email" required>
        <input type="password" name="password" id="password" placeholder="Password" required>
        
        <button type="submit" class="login_submit"></button>

        <div class="<% if (message != null && message.kind === "error") { %>errorMessage <% }else { %> validMessage <% message } %> <% if (message != null ) { %>message<% } %>">
            <% if (message != null) { %> <%= message.text %> <% } %>
        </div>
    </form>

    <%- include('partials/footer') %>

    <script>
        <% if ( token  != null && holygrail != null) { %>
            var data = <%- JSON.stringify(holygrail) %>;
            console.log(data)
            localStorage.setItem("token" , "<%- token %>")
            localStorage.setItem("holygrail" , data)
            localStorage.setItem("isDirty", false)
        <% } %>
    </script>
</body>
</html>