<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/global.css">
    <link rel="stylesheet" type="text/css" href="/css/footer.css">
    <link rel="stylesheet" type="text/css" href="/css/header.css">
    <link rel="stylesheet" type="text/css" href="/css/holygrail.css">
    <link rel="stylesheet" type="text/css" href="/css/menuholy.css"> 
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>  
    <title>Document</title>
</head>
<body>
    <%- include('partials/header') %>
    
    <%- include('partials/menuholy') %> 
    
    <h2>Runewords</h2>

    <div id="unique_content">
        <% objets.forEach(function(objet) { %>
            
            <div id=<%= objet.originalRuneWords.toLowerCase().replace(/\s/g, "_") %> class=<% if (objet.owned === true) { %>objetOwned<% }else { %> objet <% } %> onclick='obtain("<%= objet.id %>", "<%= objet.kind %>", "<%= objet.part %>", "<%= objet.value %>", "<%= objet.originalRuneWords %>", "<%= objet.item %>")'>                   
                <div class="tooltip">
                    <img src="/images/Runewords/<%= objet.image %>.png" class="obj_image">
                    <span class="obj_name"><%= objet.originalRuneWords %></span>
                    <span class="obj_type"><%= objet.runeOrder %></span>
                    <span class="obj_date"><% if (objet.dateOwned != null) { %> <%- objet.dateOwned %> <% } %></span>
                    <span class="tooltiptext"><%- objet.completedStats %></span>
                </div>
            </div>            
            
        <% }); %>        
    </div>

    <div id="btnSave" onclick="saveHolyGrail()">
    </div>

    <%- include('partials/footer') %>  
    
    <script src="/js/tooltipItems.js"></script>

    <script src="/js/dateItems.js"></script>  
    
    <script src="/js/loadRunewords.js"></script>
</body>
</html>